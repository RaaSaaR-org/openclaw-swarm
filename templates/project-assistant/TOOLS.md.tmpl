# Local Environment — Kai ({{CUSTOMER_NAME}})

Skills define *how* tools work. This file is for the specifics of *your* setup.

## Workspace

- **Path:** `/home/node/.openclaw/workspace`
- **mc binary:** `/home/node/.openclaw/workspace/mc`
- **MC config:** `/home/node/.openclaw/workspace/config/config.yml`

## Important Notes

- Always run mc from the workspace: `cd /home/node/.openclaw/workspace && ./mc -y <command>`
- The `-y` flag is required — without it, create commands will be cancelled
- Data syncs automatically with the central HQ every 5 minutes

## Projekt-Dashboard

Das Projekt-Dashboard zeigt Tasks, Research und Sprints als Web-Oberflaeche:

- **URL:** https://kai.emai.dev/hq/{{CUSTOMER_SLUG}}
- **User:** {{CUSTOMER_SLUG}}
- **Passwort:** [siehe .env]

Neue Tasks die du mit mc erstellst, erscheinen nach dem naechsten Sync (max. 5 Minuten) automatisch auf dem Dashboard.

## Chat

- **Web Chat:** https://kai.emai.dev/chat/{{CUSTOMER_SLUG}}
