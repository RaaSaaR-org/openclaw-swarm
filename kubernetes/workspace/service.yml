apiVersion: v1
kind: Service
metadata:
  name: workspace
  namespace: emai-swarm
  labels:
    app: workspace
    component: web
spec:
  type: ClusterIP
  selector:
    app: workspace
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
