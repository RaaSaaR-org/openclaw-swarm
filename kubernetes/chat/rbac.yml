# chat reads:
#   - kaiinstances (to look up the per-customer gateway URL + token)
#   - kai-<slug>-users Secrets (to verify email + password)
#   - kai-<slug>-chat-bridge Secrets (device keypair + JWT signing secret)
# It never writes — admin writes happen via workspace.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: chat
rules:
  - apiGroups: ["swarm.emai.io"]
    resources: ["kaiinstances"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: chat
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: chat
subjects:
  - kind: ServiceAccount
    name: chat
    namespace: swarm-system
