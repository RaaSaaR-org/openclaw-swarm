apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-chat
  namespace: emai-swarm
  labels:
    app: customer-chat
    component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-chat
  template:
    metadata:
      labels:
        app: customer-chat
        component: web
    spec:
      serviceAccountName: customer-chat
      containers:
        - name: customer-chat
          image: ghcr.io/raasaar-org/openclaw-swarm/customer-chat:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ADDR
              value: ":8080"
            - name: SWARM_NAMESPACE
              value: "emai-swarm"
          resources:
            # argon2id (64 MiB working set per concurrent login) needs headroom.
            requests:
              memory: "96Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            capabilities:
              drop: ["ALL"]
