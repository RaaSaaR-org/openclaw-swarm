apiVersion: v1
kind: Service
metadata:
  name: admin-console
  namespace: emai-swarm
  labels:
    app: admin-console
    component: web
spec:
  type: ClusterIP
  selector:
    app: admin-console
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
