apiVersion: v1
kind: Service
metadata:
  name: onboarding
  labels:
    app: onboarding
    component: web
spec:
  type: ClusterIP
  selector:
    app: onboarding
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
