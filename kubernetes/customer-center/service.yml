apiVersion: v1
kind: Service
metadata:
  name: customer-center
  namespace: emai-swarm
  labels:
    app: customer-center
    component: web
spec:
  type: ClusterIP
  selector:
    app: customer-center
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
